server:
  port: 8787

spring:
  application:
    name: CATEGORY

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    xyz:
      jiang:
        client: debug